{
  // These tasks will run in order when initializing your CodeSandbox project.
  "setupTasks": [],

  // These tasks can be run from CodeSandbox. Running one will open a log in the app.
  "tasks": {
    "rustup target add wasm32-unknown-unknown": {
      "name": "rustup target add wasm32-unknown-unknown",
      "command": "rustup target add wasm32-unknown-unknown",
      "runAtStart": true
    },
    "apt-get install gcc": {
      "name": "apt-get install gcc",
      "command": "apt-get install gcc",
      "runAtStart": true
    },
    "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh": {
      "name": "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh",
      "command": "curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh",
      "runAtStart": true
    },"wasm-pack build --out-dir worker-module --target web": {
      "name": "wasm-pack build --out-dir worker-module --target web",
      "command": "wasm-pack build --out-dir worker-module --target web",
      "runAtStart": true
    }
  }
}
